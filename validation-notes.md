# 介面驗證紀錄

## 2026-08-26

已以本機檔案方式開啟 `busapp-improved.html`，並確認初始化後的收藏頁可正常轉為既有的空收藏狀態。現有三個分頁控制項、刷新與編輯按鈕、匯出／匯入及清除快取按鈕仍可被瀏覽器辨識。

視覺覆寫已呈現深墨色操作列、黃色圖標、ETA 導向的主標題、暖白紙感底色、票據式空狀態和細邊操作按鈕。這次驗證尚未新增或移除任何 API、IndexedDB、PWA、分頁或事件處理程式。

搜尋分頁可正常切換，並成功顯示既有本機資料庫就緒狀態與路線／地點輸入欄。規劃分頁亦可正常切換，保留起點、定位、終點與直達路線搜尋操作，且以新的行程票據樣式呈現。

驗證期間曾在規劃分頁的起點欄輸入測試字串，之後能無誤切回路線搜尋分頁；此結果確認本次 CSS 覆寫沒有改變既有 `switchTab` 的 DOM 狀態管理。

以路線號碼 `70` 驗證後，原有搜尋邏輯成功在本機資料庫回傳 69 個相關結果，且動態產生的結果卡可正常呈現新的窄邊資訊條目樣式。驗證後移除了位於收藏頁尾端、容易與操作按鈕分離的次要說明文字；此變動僅影響 CSS 偽元素，並不改變 HTML 或程式行為。

最後以原始檔和改良檔的最後一個內嵌應用程式腳本作雜湊與內容比對，結果一致。代表 API 呼叫、IndexedDB 快取、PWA、搜尋、收藏、ETA 更新、地圖與事件處理邏輯均未修改。

## HTML 預覽整合驗證

首頁已由 React 預覽殼層直接載入 `/busapp-improved.html`。已在公開 HTML 路徑驗證收藏頁可初始化，並可切換至搜尋頁；搜尋頁成功顯示既有本機資料庫「3,764 條路線已就緒」狀態與原有路線／地點輸入欄。桌面與 375px 行動版截圖均確認時刻表式空狀態、導覽與操作按鈕可讀。

同一公開 HTML 頁面亦可切換至規劃分頁，且起點、終點、定位與「搜尋直達路線」控制項均存在並可辨識。

## 首頁標題精簡驗證

已移除首頁「下一班車，先看這裡。」與 `ROUTE／目的地／上車站／NEXT` 欄位標題。桌面預覽確認保留未儲存路線提示、新增路線入口、匯出備份、匯入資料與清除快取操作區。

## 車站群組驗證前置檢查

群組版本的公開 HTML 可正常載入，並可由收藏頁切換至搜尋頁；搜尋頁仍顯示本機路線資料庫已就緒，足以建立真實的車站收藏項目作群組驗證。

以真實路線關鍵字 `1` 搜尋後，公開 HTML 成功呈現相關路線清單；即將從該清單選取真實車站，驗證收藏區會以站名作為群組標題。

已選取九巴路線 1 的「竹園邨總站 (WT916)」並儲存為收藏項目。此操作使用原有的收藏資料與 ETA 流程，下一步將檢查該項目在收藏頁的群組顯示。

瀏覽器頁面切換後未保留前一步的收藏資料，故以同一條真實九巴路線與站點資料寫入既有 `myBusRoutes_v3` 儲存格式，並重新載入公開 HTML 作車站群組渲染驗證。此測試不會更改專案程式或發布版本的預設資料。

重新載入後，收藏頁成功呈現 `STOP 竹園邨總站 (WT916)` 群組標題、群組數量 1、路線 1 的 ETA 條目及窄欄版面。此結果確認車站名稱分組的渲染邏輯正常運作。

行動版預覽確認窄欄模式仍完整保留收藏、搜尋、規劃和操作按鈕；未有橫向溢位或可讀性問題。

## 壓縮 ETA 卡片驗證

以真實九巴路線 1 及「竹園邨總站 (WT916)」測試資料重新載入公開 HTML，檢查群組路線卡的高度與 ETA 標籤。此資料僅存在於瀏覽器測試狀態，不是專案預設資料。

驗證結果：群組內的路線卡已縮至約 42px（原約 82px），`NEXT` 文字已完全移除；路線編號、目的地、車費與到站狀態仍完整可讀。

## 雙 ETA 與就到提示驗證

使用九巴公開 ETA 資料確認路線 1 於車站 `18492910339410B1` 可回傳多班即時到站資料。已將該有效路線資料暫存於瀏覽器測試狀態，重新載入公開 HTML 以確認卡片只呈現最近兩個 ETA。

實際渲染結果僅顯示最近兩班 ETA（測試時為 13 與 31 分鐘），並以兩行右對齊呈現。`就到` 使用專屬的 `eta-arriving-pulse` 類別與降低動態偏好設定保護，讓即將到達狀態能以紅色閃動顯示而不影響使用者的減少動態偏好。

## Light Rail 與 LRT Feeder ETA 問題調查

目前 HTML 只為九巴、城巴、嶼巴和專線小巴建立 ETA 分支，確認未處理 `lightRail` 與 `lrtfeeder`。上游 `hk-bus-eta` 原始碼使用 `GET /v1/transport/mtr/lrt/getSchedule?station_id=…&with_special=1` 取得輕鐵資料，並以 `POST /v1/transport/mtr/bus/getSchedule`（`{ language, routeName }`）取得港鐵巴士資料。現有資料庫亦確實使用 `lightRail`、`lrtfeeder` 營辦商代碼，且輕鐵站點 ID 為 `LR…` 格式。

外部依據：上游輕鐵實作為 <https://github.com/hkbus/hk-bus-eta/blob/master/src/lightRail.ts>；港鐵巴士實作為 <https://github.com/hkbus/hk-bus-eta/blob/master/src/lrtfeeder.ts>；官方輕鐵服務端點為 <https://rt.data.gov.hk/v1/transport/mtr/lrt/getSchedule>；官方港鐵巴士服務端點為 <https://rt.data.gov.hk/v1/transport/mtr/bus/getSchedule>。

已用真實輕鐵資料驗證：路線 505、三聖站 `LR920` 成功顯示 ETA（測試時為「就到」及 13 分鐘）。收藏區的可見「更新 ETA」按鈕亦已出現，更新後會顯示短暫完成時間。

已用真實港鐵巴士資料驗證：路線 506、兆麟站 `506-D010` 成功經官方 POST 資料服務顯示兩個 ETA（測試時為 10 與 22 分鐘）。輕鐵與港鐵巴士的收藏 ETA 分支均已成功回傳資料。

手動「更新 ETA」按鈕已於港鐵巴士測試中完成驗證：點擊後文字改為「更新中…」、按鈕暫時禁用並顯示「正在取得最新 ETA」；完成後恢復可用，ETA 由 10／22 分鐘更新為 9／21 分鐘。

本機搜尋驗證：查詢路線 `505` 時，結果已正確列出輕鐵 505 往兆康及往三聖的路線，並以「輕鐵」標識營辦商。因此修正同時支援由搜尋結果選擇車站、儲存收藏及後續 ETA 查詢。

## 輕鐵及港鐵巴士地圖問題調查

已確認路線資料庫本身包含所需座標：輕鐵三聖站 `LR920` 為 `22.3829, 113.97686`，港鐵巴士兆麟站 `506-D010` 為 `22.38551, 113.9767`。因此地圖未顯示的原因是 `openMapModal` 僅建立九巴、城巴、嶼巴及專線小巴的站點清單，未為 `lightRail` 和 `lrtfeeder` 從本機 `routeList`／`stopList` 建立地圖站點。

輕鐵地圖驗證以路線 505、三聖站 `LR920` 進行；地圖開啟前收藏卡已成功顯示 ETA（測試時為 11 分鐘）。

輕鐵 505 地圖已成功開啟並載入 16 個沿途站點（由三聖至兆康），包括站點清單、地圖標記與路線折線。其後已切換為港鐵巴士 506、兆麟站 `506-D010` 的測試收藏，準備核對第二類服務的地圖呈現。

港鐵巴士 506 地圖亦已成功開啟並載入 14 個沿途站點（由兆麟至屯門碼頭），包括 Google 街道圖圖磚、地圖標記、路線折線及站點清單；收藏卡同時維持兩個 ETA（測試時為 7 與 17 分鐘）。

車站群組卡片已套用四邊 12px 圓角；瀏覽器計算後的 `border-radius` 驗證值為 `12px`。

## 舊收藏 ETA 與營辦商顏色相容性

以舊格式收藏測試港鐵巴士：營辦商別名 `irtfeeder` 與站點欄位 `mtrfeeder_stop: 506-D010` 成功正規化為 LRT Feeder，並顯示官方 ETA（測試時為「就到」及 8 分鐘）。卡片的邊條與路線徽章均套用指定橙色。

以舊格式收藏測試輕鐵：營辦商別名 `lrt` 與站點欄位 `lrt_stop: LR920` 成功正規化為 Light Rail，並顯示官方 ETA（測試時為 4 及 16 分鐘）。路線徽章為白色，文字與細框使用深墨色以維持清晰對比。

最新瀏覽器驗證確認輕鐵卡片具有 `op-lightRail` 類別，ETA 顯示 3 與 15 分鐘；其路線徽章背景為白色（`rgb(255,255,255)`），文字與邊框為深墨色。港鐵巴士舊格式驗證則確認 ETA 和橙色徽章均正常。

## hk-independent-bus-eta 對照修正

上游 `useEtas` 會把完整 `routeObj`、序號及 `stopList` 一併傳入正規化 ETA 函式。現已為舊收藏加入相同概念的補全步驟：以路線號、站名、營辦商及本機 `routeList`／`stopList` 自動取得正確的 route key、目的地與站點 ID。驗證中僅提供 `{ stopName: '三聖', route: '505', operators: ['lrt'] }` 的輕鐵舊收藏，成功補全並顯示 10 分鐘 ETA。

同一補全流程亦用於港鐵巴士：僅提供 `{ stopName: '兆麟', route: '506', operators: ['irtfeeder'] }` 的舊收藏，即成功補全為 LRT Feeder 正確站點並顯示 4 與 14 分鐘 ETA。

## 搜尋站點列表 ETA 修正

公開搜尋頁已確認載入 3764 條本機路線資料，現以港鐵巴士 506 重現站點列表全站 ETA 載入流程。

已開啟 506 往屯門碼頭的 14 個站點列表；初始畫面依預期先顯示「載入中…」，後續檢查將確認各列是否完成更新。

全站查詢已完成，列表不再停留在「載入中…」，但全部列為「暫無班次」。檢查搜尋流程可見正確 route key `506+1+Siu Lun+Tuen Mun Ferry Pier` 與站點鍵（例如 `506-D010`、`506-D020`）；因此需繼續核對港鐵巴士時刻表回應的站點資料結構與匹配條件。

已重新載入最新版本並重返路線搜尋頁，準備以 506 確認等待式全站 ETA 載入的首次結果。

最新測試已重新取得 506 搜尋結果，並選定港鐵巴士「往屯門碼頭」方向，下一步檢查站點列表首次載入後的 ETA 渲染。

等待式載入已確保列表流程完成，但首次全站請求仍統一顯示「暫無班次」，因此根因不是載入時序。應用程式輔助函式與官方回應均可取得 `506-D010` 的班次資料，下一步改為抽出並共用港鐵巴士站點 ETA 映射，以消除搜尋列表與收藏頁的資料處理差異。

港鐵巴士全站 ETA 現已加入一次短暫重試。以目前站點列表手動載入與正常搜尋結果選取流程分別驗證，前四站均直接顯示 ETA；例如兆麟為 2／10 分鐘、友愛邨為 4／12 分鐘、輕鐵市中心站為 5／13 分鐘、輕鐵屯門站為 7／15 分鐘，列表不再停留在「載入中」或「暫無班次」。

完整 506 列表亦已確認 14 個站點均完成 ETA 呈現；現已返回結果清單，改以輕鐵路線驗證全站載入分支。

2026-08-26 10:40 的官方輕鐵回應確認：三聖站 `LR920` 以 `station_id=920` 查詢時，505 往兆康資料位於 `platform_list[0].route_list[0]`，欄位為 `route_no: "505"`、`dest_ch: "兆康"`、`time_en: "8 mins"`、`stop: 0`。端點及路線／目的地篩選條件正確，後續修正集中於搜尋列表的完成時序與重試流程。

公開搜尋結果已成功定位真實的 `505 往兆康／由三聖開出（輕鐵）` 選項，並確認它使用既有 `app.selectRouteDirection(2)` UI 選取流程。下一步以此原始互動流程觀察 16 個站點的 ETA 更新完成狀態。

以原始搜尋 UI 選取 505 往兆康後，初始畫面確實會讓全部 16 站同時顯示「載入中…」。等待 10 秒後，前 15 站已顯示 ETA（例如三聖 5／17、兆麟 7／19、屯門「就到」／12），兆康則正確顯示「暫無班次」。因此資料與渲染函式可用，問題是目前每四站串行查詢、且空值會重試一次，造成首次顯示延遲過長。

已將輕鐵搜尋列表分支改為對全部站點同步發出 ETA 查詢；每個站點仍保留一次 280ms 後的獨立重試與失敗降級。這會移除四個批次的串行等待，同時保留官方回應偶發空值時的防護。

更新後已重新載入公開頁面、搜尋 505 並以既有 UI 選取輕鐵 505 往兆康；下一步將量測更新後各站的完成結果。

更新後實測結果：3.5 秒內 505 往兆康全線 16 站均已結束載入狀態。三聖顯示 3／15、兆麟 5／17、建安「就到」／12、山景（南）2／14、良景「就到」／8；終點兆康正確顯示「暫無班次」。視覺列表已確認不再殘留「載入中…」。

## 車站群組拖曳排序驗證

已建立僅限瀏覽器驗證工作階段的收藏資料：竹園邨總站（2 條路線）、天主教伍華中學（1 條路線）及九龍灣站（1 條路線）。此測試資料不會寫入專案預設內容，用以驗證整個車站群組與群組內路線的排序、保存及重載行為。

公開頁面已載入 3 個測試車站群組及 4 條路線。點擊既有「編輯路線」後，三個群組標題均顯示具提示文字「拖曳排序車站」的獨立把手，群組內既有路線拖曳把手與刪除按鈕仍同時可見。

已完成一次群組排序驗證，將「九龍灣站」移至最前。排序回呼成功重新渲染為「九龍灣站 → 竹園邨總站 → 天主教伍華中學」，且本機收藏資料同步保存為 `九龍灣站:5、竹園邨總站:1、竹園邨總站:116、天主教伍華中學:3D`。群組順序與群組內路線順序均維持正確。

另外已定位含兩條路線的「竹園邨總站」群組，並以其既有路線 Sortable 實例觸發群組內重新排序回呼；下一步將核對重繪與保存後的完整順序。

群組內路線回歸驗證通過：在維持群組順序「九龍灣站 → 竹園邨總站 → 天主教伍華中學」下，竹園邨總站的兩條路線成功改為 `116 → 1`，本機收藏資料亦相應保存為 `九龍灣站:5、竹園邨總站:116、竹園邨總站:1、天主教伍華中學:3D`。新增群組排序沒有影響既有的群組內路線排序。

## 大嶼山巴士地圖 ETA 驗證

官方資料確認 3M 東涌站巴士總站往梅窩碼頭的 `routeId` 為 `6`；散石灣村站點為 `stopId: 257`。官方 ETA 端點 `estimatedArrivals&routeId=6&stopId=257` 可回傳多班 `estimatedArrivalTime`。已在僅限瀏覽器驗證工作階段建立相同的 3M 收藏資料，準備開啟地圖確認 UI 渲染。

已成功開啟 3M（routeId 6）沿途地圖，並確認地圖列表取得 34 個官方站點與正確站點 ID。首次渲染時所有 ETA 區塊均顯示「載入中…」，下一步將觀察完成後的資料對應與延遲。

原始實測在 11 秒後已讓 3M 地圖的 34 個站點全部結束載入，其中多數顯示官方 ETA，少數無服務站正確顯示「暫無班次」。為處理截圖所示的舊收藏資料情境，現已加入 `routeId` 自動補全；以未含 `nlb_routeId` 的舊格式 3M 收藏重載後，散石灣村收藏卡仍成功顯示 480／505 分鐘，確認可依路線與起訖點恢復正確 routeId。

以補全後的舊格式 3M 收藏再次開啟地圖，散石灣村選定站即時顯示 480／504 分鐘；7.5 秒後 34 個沿途站點全部結束載入，32 個顯示 ETA、2 個正確顯示「暫無班次」，沒有殘留「載入中…」。

## 大嶼山巴士 11 搜尋 ETA 調查

官方資料確認「11 大澳往東涌站巴士總站」的 `routeId` 為 `14`，水口村（東）的 `stopId` 為 `240`。官方 ETA 端點以此組合回傳三班 ETA（12:25:55、12:45:55、13:10:55），故服務端有可用即時資料。現正以公開搜尋頁輸入 11，核對應用程式實際選用的路線方向與 routeId。

使用者截圖所示站序（水口村〈東〉、下橫壟、上橫壟）對應官方 11「大澳〈水口開〉往東涌」特別班次 `routeId: 18`。此特別班次的 ETA 端點不回傳班次，而同方向常規班次 `routeId: 14` 可回傳即時資料；現已加入 ETA 專用的常規方向回退邏輯。已從公開搜尋結果選取相同的 11 水口開方向，開始驗證修正後的列表。

修正後的列表仍統一顯示「暫無班次」，但在同一瀏覽器直接呼叫官方 `routeId=14&stopId=240` 時，確實回傳 12:25:30、12:45:55、13:10:55。這證實問題在應用程式實際送出的站點／路線對應，將繼續攔截頁面請求以定位差異。

已在公開頁面啟用大嶼山巴士請求記錄，並重新選取相同的「11 往東涌、由大澳（水口開）開出」結果；下一步將讀取頁面實際發出的 ETA URL。

請求記錄顯示目前已開啟頁面仍對 31 個站點發出 `routeId=18` ETA 請求，證實該瀏覽器分頁是在程式更新前載入，尚未套用 ETA 專用回退程式。將重新載入公開 HTML，再確認更新版會改用常規 `routeId=14`。

已重新載入公開 HTML 並重新進入搜尋頁；接下來會以新版程式重新選取 11 水口開往東涌方向，核對 ETA 請求與顯示結果。

最新版公開頁已載入，並已重新選取「11 往東涌、由大澳（水口開）開出」；等待 ETA 更新後檢查首站與全站結果。

新版仍查詢 `routeId=18`，原因已定位：官方路線資料將 11 水口開的 `specialRoute` 也標示為 `0`，無法單靠該旗標識別。將改以「起點名稱含括號變體（例如大澳〈水口開〉）」辨識此類短線／特別起點，並在 ETA 查詢使用相同目的地的常規起點路線 `routeId=14`。

已重新載入包含起點變體修正的公開 HTML，並回到搜尋頁；即將重新選取同一路線驗證 ETA 顯示。

已在最新版中再次選取 11 水口開往東涌方向，等待 ETA 回應後核對水口村（東）及後續站點顯示。

最終驗證通過：5 秒內站點列表不再有「載入中…」，水口村（東）顯示 500／525 分鐘、下橫壟 500／525、上橫壟 501／526、南大嶼山海底電纜站 481／502。請求記錄確認已改用常規 `routeId=14`，修正了水口開 `routeId=18` 官方 ETA 空回應造成的全列「暫無班次」。

## 路線 123 確認

大嶼山巴士官方路線清單未有 `123`。已在應用程式搜尋頁重新確認，下一步將依搜尋結果辨識實際營辦商與方向，避免把嶼巴 ETA 規則錯用到其他營辦商。

## 頂部功能列香港實時時間

已在深色功能列右側加入 `HK HH:MM:SS` 實時時間，使用 `Asia/Hong_Kong` 時區格式化，並採等寬數字避免秒數變動造成版面跳動。公開頁驗證時間由 13:20:46 更新至 13:20:47，確認每秒刷新；小螢幕會隱藏 HK 標籤以保留導覽與功能按鈕空間。
